# 🚀 Django Task Manager

A robust, resume-ready CRUD application built with Django 5.0. It features a complete Task Management system with secure user authentication, database persistence, and a responsive custom UI.

## 🌟 Features
* **Full CRUD:** Create, Read, Update, and Delete tasks.
* **Authentication:** Secure Login/Logout logic.
* **User Isolation:** Users can only see and edit their own tasks.
* **Database:** Configured for MySQL (Production) and SQLite (Dev).
* **UI/UX:** Clean, minimalist interface using CSS3 and Semantic HTML.

## 🛠️ Tech Stack
* **Backend:** Python, Django
* **Database:** MySQL / SQLite
* **Frontend:** HTML5, CSS3, Django Template Language (DTL)

## 📦 Setup Instructions
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run migrations: `python manage.py migrate`
4. Create superuser: `python manage.py createsuperuser`
5. Run server: `python manage.py runserver`
